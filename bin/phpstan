#!/bin/bash
BASEDIR="$(dirname $(realpath $0))/.."
docker run --rm -v "${BASEDIR}"/sequra:/app -w /app -u $(id -u):$(id -g) php:cli-alpine php vendor/bin/phpstan analyse -c phpstan.neon $@ --memory-limit=512M