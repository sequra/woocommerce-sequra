#!/bin/bash
BASEDIR="$(dirname $(realpath $0))/.."
cd "${BASEDIR}"

cd "${BASEDIR}" || exit
set -o allexport
source .env
set +o allexport

if [ -z "$PUBLIC_URL" ]; then
	PUBLIC_URL="$WP_URL"
fi

cd sequra
npx playwright test $@
