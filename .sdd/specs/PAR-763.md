---
spec_id: PAR-763
spec_type: chore
spec_title: Register StoreIntegration Via Migration
branch: chore/PAR-763-Register-StoreIntegration-Via-Migration
source: jira
source_ref: PAR-763
---

# Spec: Register StoreIntegration Via Migration

## Context / Background
The plugin configuration needs to be accessible through the seQura Merchant Portal. Currently, the StoreIntegration is not properly registered, preventing the Merchant Portal from accessing the plugin configuration. A migration is needed for version 4.3.0 to fix this.

## Summary
Implement a new migration for version 4.3.0 that ensures the StoreIntegration is properly registered so the plugin configuration is accessible through the Merchant Portal.

## Functional Requirements
- A new migration must register the StoreIntegration for the plugin
- The migration must run as part of the 4.3.0 version upgrade path
- After migration, the plugin configuration must be accessible through the Merchant Portal

## Non-Goals / Out of Scope
- Changes to the Merchant Portal itself
- Modifications to the StoreIntegration logic beyond registration

## Possible Edge Cases
- Existing installations that already have a correctly registered StoreIntegration
- Multisite installations where registration must apply per-store
- Migration running on a fresh install vs an upgrade

## Acceptance Criteria
- The migration executes successfully on upgrade to 4.3.0
- The StoreIntegration is registered after migration completes
- Plugin configuration is accessible through the Merchant Portal
- Existing correctly-registered installations are not disrupted

## Open Questions
- ...

## Dependencies
- ...

## Success Metrics
- ...

## Testing Guidelines
- ...

## Original Description
Implement a new migration for version 4.3.0 to make sure the StoreIntegration is properly registered so the plugin configuration can be accessible through the Merchant Portal

## Clarifications

### 2026-04-22 — re-run #2 clarifications

- **Store loop (StoreIntegrationMigrateTask pattern):** Migration must iterate over all connected stores via `StoreService::getConnectedStores()`, wrapping each iteration in `StoreContext::doWithStore($storeId, ...)`. Current code only processes the default store context — wrong for multisite.
- **StoreService dependency:** Use `ServiceRegister::getService(StoreService::class)` inside `execute()` (matches core task pattern) rather than constructor injection.
- **Skip existing:** Within each store context, check `StoreIntegrationRepositoryInterface::getStoreIntegration()` before calling `createStoreIntegration()`. Skip if non-null.
- **Failure handling:** Catch per-connection, log, continue. After the full store+connection loop, throw `Exception` if any failures (triggers retry on next plugin execution).
- **Unit tests:** Test via mocked services — verify `createStoreIntegration` called correct number of times with right data. Don't verify storeId scoping directly (static `StoreContext::doWithStore` hard to mock).

### 2026-04-22 — re-run #1 clarifications

- **Skip existing:** Before calling `createStoreIntegration()`, check `StoreIntegrationRepositoryInterface::getStoreIntegration()`. If a record already exists, skip the API call entirely for that connection.
- **Loop resilience:** A failure on one connection must not abort the loop; catch the exception, log it, continue to the next connection.
- **Retry signal:** If any connection failed, throw a plain `Exception` (not `Critical_Migration_Exception`) after the loop completes. This causes `Migration_Manager` to skip `set_module_version('4.3.0')`, so the migration retries on the next plugin execution.

### Initial clarifications

- **Failure handling:** Non-critical — log error and continue. Plugin still works without Merchant Portal access.
- **No ConnectionData:** Return early silently — registration will happen naturally during onboarding.
- **Migration version:** `4.3.0` as stated in the ticket.

## Analysis

### Affected Files

**Migration layer:**
- `sequra/src/Repositories/Migrations/class-migration-install-430.php` — **created** (Step 1); rewritten (Step 7) to follow `StoreIntegrationMigrateTask` pattern: loop over connected stores via `StoreService::getConnectedStores()` + `StoreContext::doWithStore()`, skip existing, track failures, throw after loop. _(2026-04-22: StoreService resolved via `ServiceRegister::getService()` inside execute, not constructor-injected. StoreIntegrationRepositoryInterface also resolved at runtime.)_
- `sequra/tests/Repositories/Migrations/MigrationInstall430Test.php` — **new file** (Step 8), unit tests following the `MigrationInstall420Test` pattern.

**Bootstrap/registration:**
- `sequra/src/class-bootstrap.php` — _(2026-04-22: simplified)_ Remove `StoreIntegrationRepositoryInterface` constructor arg from `Migration_Install_430` instantiation. Only pass: `$wpdb`, `$cache_repository`, `ConnectionService`, `StoreIntegrationService`, `Interface_Logger_Service`. Runtime resolution of `StoreService` and `StoreIntegrationRepositoryInterface` inside execute() eliminates need to pass them through Bootstrap.

### Risks & Concerns

- **API failure during migration:** The `StoreIntegrationsProxy::createStoreIntegration()` call hits the seQura API. If the API is down or credentials are invalid, the migration will throw. Mitigation: catch the exception, log it, and continue (non-critical). After the loop, throw `Exception` so the migration retries on next plugin execution.
- **Idempotency:** `getStoreIntegration()` check before the API call ensures already-registered connections are not re-sent to the API. Safe to run multiple times.
- **Multisite:** _(2026-04-22: resolved)_ Migration now explicitly loops over all connected stores using `StoreService::getConnectedStores()` + `StoreContext::doWithStore()`. Each store is processed in its own context. Previous version only processed default store context — that was the bug.
- **Retry loop:** Throwing `Exception` prevents `set_module_version('4.3.0')` from being saved, so partially-registered stores will retry. Already-registered stores are skipped on retry (idempotent check). Risk is bounded.
- **Store context scoping correctness:** _(2026-04-22)_ `ConnectionService::getAllConnectionData()` and `StoreIntegrationRepositoryInterface::getStoreIntegration()` both read from repositories scoped by `StoreContext`. Using `doWithStore()` ensures the correct storeId is active when these calls execute. Critical to get right.

### Decisions

- Follow Migration_Install_420 pattern for class structure: constructor injection for services used by migration orchestration.
- _(2026-04-22: changed)_ Use `ServiceRegister::getService(StoreService::class)` and `ServiceRegister::getService(StoreIntegrationRepositoryInterface::class)` inside `execute()` — matches the core `StoreIntegrationMigrateTask` pattern. Avoids adding constructor params that are only needed for store-context-scoped logic.
- Use `ConnectionService::getAllConnectionData()` to retrieve connections (new format, post-4.0.0).
- Call `StoreIntegrationService::createStoreIntegration()` for each ConnectionData.
- Wrap in try/catch per connection so one failure doesn't block others.
- _(2026-04-22)_ Use a `$has_failures` flag: set to `true` on any caught exception, throw `Exception` (not `Critical_Migration_Exception`) after the loop if `$has_failures` is `true`.

## Implementation Plan
- [x] ~~Step 1: Create `sequra/src/Repositories/Migrations/class-migration-install-430.php` — migration class with version `4.3.0`, injecting `ConnectionService` and `StoreIntegrationService`, iterating connections and calling `createStoreIntegration()`.~~ _(superseded: migration logic requires store loop and skip-existing check)_
- [x] Step 2: Register `Migration_Install_430` in `sequra/src/class-bootstrap.php` — add to the migrations array after `Migration_Install_420`, passing the required service dependencies.
- [x] Step 3: Run QA pipeline — `./bin/phpcs`, `./bin/phpstan`, and unit tests via Docker.
- [x] ~~Step 4: Refactor `sequra/src/Repositories/Migrations/class-migration-install-430.php` — inject `StoreIntegrationRepositoryInterface`; before each `createStoreIntegration()` call check `getStoreIntegration()` and skip if non-null; track failures with `$has_failures`; after the loop throw `new Exception(...)` if `$has_failures` is `true`.~~ _(superseded: replaced by Step 7 — store loop approach with runtime service resolution)_
- [x] ~~Step 5: Update `sequra/src/class-bootstrap.php` — add `StoreIntegrationRepositoryInterface` import and pass to `Migration_Install_430` constructor.~~ _(superseded: replaced by Step 8 — StoreIntegrationRepositoryInterface resolved at runtime, not injected)_
- [x] ~~Step 6: Create `sequra/tests/Repositories/Migrations/MigrationInstall430Test.php` — unit tests covering skip/fail/success paths.~~ _(superseded: replaced by Step 9 with store-loop test scenarios)_
- [x] Step 7: Rewrite `sequra/src/Repositories/Migrations/class-migration-install-430.php` — remove `StoreIntegrationRepositoryInterface` constructor param; in `execute()`: resolve `StoreService` and `StoreIntegrationRepositoryInterface` via `ServiceRegister::getService()`; loop over `$store_service->getConnectedStores()`; wrap each iteration in `StoreContext::doWithStore($store_id, ...)`; within callback get `getAllConnectionData()`, skip connections where `getStoreIntegration()` is non-null, call `createStoreIntegration()` for missing ones; catch per-connection, log, set `$has_failures`; after full loop throw `Exception` if any failures. Update `sequra/src/class-bootstrap.php` — remove `StoreIntegrationRepositoryInterface` arg from `Migration_Install_430` constructor call (keep 5 args: `$wpdb`, `$cache_repository`, `ConnectionService`, `StoreIntegrationService`, `Interface_Logger_Service`).
- [x] Step 8: Create `sequra/tests/Repositories/Migrations/MigrationInstall430Test.php` — unit tests following `MigrationInstall420Test` pattern. Cases: `get_version()` returns `4.3.0`; no connected stores → early return, no service calls; store with existing StoreIntegration → skips `createStoreIntegration()`; store without StoreIntegration → calls `createStoreIntegration()` with correct `ConnectionData`; multiple stores, one connection fails → continues loop, throws `Exception` after; all succeed → no exception thrown.
- [x] Step 9: Run QA pipeline — `./bin/phpcs`, `./bin/phpstan`, and unit tests via Docker.
